import 'package:flutter/material.dart';

class SatelliteMapScreen extends StatelessWidget {
  const SatelliteMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Satellite Map Screen'),
    );
  }
}